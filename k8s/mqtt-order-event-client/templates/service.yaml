apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqtt-order-event-client.fullname" . }}
  labels:
    {{- include "mqtt-order-event-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mqtt-order-event-client.selectorLabels" . | nindent 4 }}
