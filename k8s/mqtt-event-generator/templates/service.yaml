apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqtt-event-generator.fullname" . }}
  labels:
    {{- include "mqtt-event-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mqtt-event-generator.selectorLabels" . | nindent 4 }}