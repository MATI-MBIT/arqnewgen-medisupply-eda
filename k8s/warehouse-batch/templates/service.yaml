apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-batch.fullname" . }}
  labels:
    {{- include "warehouse-batch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warehouse-batch.selectorLabels" . | nindent 4 }}